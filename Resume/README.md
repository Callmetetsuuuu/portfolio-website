Drop resume files here (PDF, DOCX, PNG). Add a file named `resume.pdf` or `resume.docx` to make it downloadable.

This folder is public when served; replace this README with your actual resume files.
